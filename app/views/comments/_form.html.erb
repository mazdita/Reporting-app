<%= form_with(model: @comment, local: true) do |f| %>
    <%= f.text_area :content, size: "20x5", class: 'form-control' %>   
    <%= f.hidden_field :report_id, value: params[:id]%>     
    <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>